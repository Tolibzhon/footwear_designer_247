import 'package:flutter/material.dart';

class SelectMaterial extends StatelessWidget {
  const SelectMaterial({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Select Material"),
      ),
      body: const Placeholder(),
    );
  }
}
